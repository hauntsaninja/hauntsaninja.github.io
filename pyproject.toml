[project]
name = "sssg"
version = "0.1"
description = "simple static site generator"
dependencies = [
    "mistletoe>=1.5.1",
    "pygments>=2.19.2",
    "python-dateutil",
]

[build-system]
requires = ["flit_core >=3.8"]
build-backend = "flit_core.buildapi"
